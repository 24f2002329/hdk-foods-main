name: hdk_core
description: "Shared core code, models, api client, and UI widgets for HDK Foods apps."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  flutter_secure_storage: ^10.3.1
  google_fonts: ^6.2.1
  lottie: ^3.1.2
  firebase_core: ^4.0.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
