name: Continuous Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  formatting:
    name: Check Code Formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Check Python Formatting (Black)
        run: |
          pip install black
          black --check backend/

      - name: Check Dart Formatting
        run: |
          dart format --output=none --set-exit-if-changed packages/hdk_core
          dart format --output=none --set-exit-if-changed frontend
          dart format --output=none --set-exit-if-changed frontend_admin
          dart format --output=none --set-exit-if-changed frontend_delivery

  security-scan:
    name: Security Scan (Bandit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run Bandit Security Scan
        run: |
          pip install bandit
          bandit -r backend/ -ll

  backend-tests:
    name: Django Backend Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          python -m venv antenv
          source antenv/bin/activate
          pip install -r backend/requirements.txt

      - name: Run Django Tests
        run: |
          source ../antenv/bin/activate
          python manage.py test tests --verbosity=2 --noinput
        working-directory: backend
        env:
          DJANGO_SETTINGS_MODULE: config.settings
          SECRET_KEY: ci-test-secret-key-not-for-production
          DEBUG: "True"

  flutter-validation:
    name: Flutter Analyze, Test & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies (hdk_core)
        run: flutter pub get
        working-directory: packages/hdk_core

      - name: Install dependencies (frontend)
        run: flutter pub get
        working-directory: frontend

      - name: Install dependencies (frontend_admin)
        run: flutter pub get
        working-directory: frontend_admin

      - name: Install dependencies (frontend_delivery)
        run: flutter pub get
        working-directory: frontend_delivery

      - name: Flutter Analyze
        run: |
          flutter analyze packages/hdk_core --no-fatal-infos
          flutter analyze frontend --no-fatal-infos
          flutter analyze frontend_delivery --no-fatal-infos
          flutter analyze frontend_admin --no-fatal-infos

      - name: Flutter Test
        run: |
          cd frontend && flutter test
          cd ../frontend_admin && flutter test
          cd ../frontend_delivery && flutter test

      - name: Create Mock google-services.json
        run: |
          mkdir -p android/app
          echo '{"project_info":{"project_number":"123456789","project_id":"mock-id","storage_bucket":"mock-id.appspot.com"},"client":[{"client_info":{"mobilesdk_app_id":"1:123:android:123","android_client_info":{"package_name":"com.hdkfoods.app"}},"api_key":[{"current_key":"mock_key"}],"services":{"appinvite_service":{"other_platform_oauth_client":[]}}}]}' > android/app/google-services.json
        working-directory: frontend

      - name: Build Android APK (Customer App)
        run: flutter build apk --debug --dart-define=ENV=dev --dart-define=GOOGLE_MAPS_API_KEY=mock_key
        working-directory: frontend

      - name: Build Web (Admin App)
        run: flutter build web --dart-define=ENV=dev --dart-define=GOOGLE_MAPS_API_KEY=mock_key
        working-directory: frontend_admin
